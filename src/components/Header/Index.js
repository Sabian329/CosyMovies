import React from "react";

import { Search } from "../Search/Index";
import { Wrapper } from "./Styled";

export const Header = () => {
  return (
    <Wrapper>
      <Search />
    </Wrapper>
  );
};
